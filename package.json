{
  "main": "index.js",
  "scripts": {
    "test": "tsc && jest --silent ./dist",
    "coverage": "npx jest --coverage --runInBand",
    "start": "nodemon",
    "debug": "tsnd --inspect --respawn --transpile-only --ignore-watch node_modules src/Server.ts"
  },
  "devDependencies": {
    "@types/jest": "^28.1.3",
    "jest": "^28.1.1",
    "ts-jest": "^28.0.5",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "@types/express": "^4.17.13",
    "eslint": "^8.23.0",
    "express": "^4.18.1",
    "nodemon": "^2.0.18",
    "tsnd": "^1.0.2"
  }
}
